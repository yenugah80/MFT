// Base API URL
// Hard-coded to the deployed backend so mobile always hits
// https://myfoodtracker.onrender.com/api regardless of local env quirks.
export const API_URL = "https://myfoodtracker.onrender.com/api";

// Legacy export for backwards compatibility (profileAPI uses this without /api)
export const API_BASE_URL = "https://myfoodtracker.onrender.com";