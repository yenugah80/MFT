// Web stub for lottie-react-native — the native Lottie player is not available on web.
import React from 'react';
import { View } from 'react-native';

const LottieView = React.forwardRef((_props, _ref) => React.createElement(View, null));
LottieView.displayName = 'LottieView';

export default LottieView;
